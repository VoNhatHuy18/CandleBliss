export const getOrdersByStatus = async (status?: string | undefined): Promise<any> => {
  // return (aw ait API.get(`${ROUTE_API.orders}}`)) as Promise<any>
}

export const getOrderDetails = async (id: string): Promise<any> => {
  // return (aw ait API.get(`${ROUTE_API.orders}}`)) as Promise<any>
}
